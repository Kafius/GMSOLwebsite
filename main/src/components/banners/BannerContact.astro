<section class="relative bg-[#080808] py-32 lg:py-48 overflow-hidden border-b border-white/5">
  
  <div class="absolute inset-0 pointer-events-none z-50 opacity-[0.12] mix-blend-screen" 
       style="background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(255, 255, 255, 0.1) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.03), rgba(0, 255, 0, 0.01), rgba(0, 0, 255, 0.03)); background-size: 100% 4px, 3px 100%;">
  </div>

  <div class="absolute inset-0 opacity-[0.03] pointer-events-none mix-blend-overlay z-40">
    <svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
      <filter id="noiseFilter">
        <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/>
      </filter>
      <rect width="100%" height="100%" filter="url(#noiseFilter)"/>
    </svg>
  </div>

  <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_rgba(165,48,41,0.1)_0%,_transparent_70%)] pointer-events-none"></div>

  <div class="absolute bottom-[-300px] lg:bottom-[-500px] left-1/2 -translate-x-1/2 w-[600px] h-[600px] lg:w-[1000px] lg:h-[1000px] pointer-events-none z-0">
    <div class="vinyl-record w-full h-full rounded-full border border-white/10 flex items-center justify-center shadow-[0_-50px_100px_rgba(0,0,0,1)]">
      <div class="relative w-1/4 h-1/4 rounded-full bg-[#A53029] flex items-center justify-center border-[10px] border-black/30 shadow-inner">
         <div class="w-3 h-3 rounded-full bg-black"></div>
      </div>
    </div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-12 text-center">
    <div class="inline-flex items-center gap-3 mb-8 border border-white/10 px-5 py-2 rounded-full backdrop-blur-md bg-black/40">
        <div class="w-1.5 h-1.5 bg-green-500 rounded-full animate-flicker shadow-[0_0_8px_rgba(34,197,94,0.6)]"></div>
        <span class="font-mono text-white/50 text-[10px] uppercase tracking-[0.4em]">
          // System.Status: Signal_Found
        </span>
    </div>

    <h1 class="font-heading text-[11vw] lg:text-[9vw] leading-[0.8] text-white uppercase tracking-tighter drop-shadow-2xl">
      Open <br />
      <span class="text-transparent stroke-white italic relative">
        Channels
        <span class="absolute -bottom-6 left-1/2 -translate-x-1/2 w-48 h-[1px] bg-gradient-to-r from-transparent via-[#A53029] to-transparent hidden lg:block"></span>
      </span>
    </h1>

    <p class="mt-12 font-sans text-gray-400/80 text-lg lg:text-xl max-w-2xl mx-auto leading-relaxed">
      Initiate signal path. Our studio team is ready to receive your cue sheet specifications and project inquiries.
    </p>
  </div>

</section>

<style>
  .stroke-white {
    -webkit-text-stroke: 1.5px white;
  }

  /* Subtitle/Status Flicker */
  @keyframes flicker {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
    95% { opacity: 1; }
    97% { opacity: 0.8; }
  }
  .animate-flicker {
    animation: flicker 4s infinite;
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .vinyl-record {
    animation: spin 35s linear infinite;
    background: 
      conic-gradient(
        from 0deg,
        transparent 0deg,
        rgba(255,255,255,0.03) 40deg,
        transparent 80deg,
        transparent 180deg,
        rgba(255,255,255,0.03) 220deg,
        transparent 260deg
      ),
      repeating-radial-gradient(
        circle,
        #050505 0px,
        #050505 1px,
        #0a0a0a 2px,
        #050505 3px
      );
  }
</style>