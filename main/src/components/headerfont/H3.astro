---
import type { HTMLAttributes } from 'astro/types';
interface Props extends HTMLAttributes<'h3'> {}
const { class: className = '' } = Astro.props;
---
<h3 class={`font-heading text-3xl lg:text-4xl uppercase tracking-tighter ${className}`}>
  <slot />
</h3>
